{
  "name": "@pbnjam/pvm",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build:wasm": "bun run build.wasm.ts",
    "build:all": "bun run build && bun run build:wasm",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "test": "echo 'No tests in CI'"
  },
  "dependencies": {
    "@pbnjam/core": "workspace:*",
    "@pbnjam/codec": "workspace:*",
    "pino": "^9.6.0",
    "pino-pretty": "^11.0.0",
    "@pbnjam/types": "workspace:*"
  },
  "devDependencies": {
    "@pbnjam/typescript-config": "workspace:*",
    "bun-types": "1.3.9"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
