import { logger } from '@pbnj/core';
export class GenKeysCommand {
    async execute(options) {
        // Empty implementation
        logger.info('gen-keys command called with options:', options);
    }
}
//# sourceMappingURL=gen-keys.js.map