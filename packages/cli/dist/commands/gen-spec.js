import { logger } from '@pbnj/core';
export class GenSpecCommand {
    async execute(options) {
        // Empty implementation
        logger.info('gen-spec command called with options:', options);
    }
}
//# sourceMappingURL=gen-spec.js.map