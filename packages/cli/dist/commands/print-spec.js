import { logger } from '@pbnj/core';
export class PrintSpecCommand {
    async execute(options) {
        // Empty implementation
        logger.info('print-spec command called with options:', options);
    }
}
//# sourceMappingURL=print-spec.js.map