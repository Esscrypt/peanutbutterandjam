{
  "name": "@pbnjam/pvm-invocations",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build:wasm": "bun run build.wasm.ts",
    "build:all": "bun run build && bun run build:wasm",
    "check": "tsc --pretty --noEmit",
    "test": "vitest run",
    "test:watch": "vitest run --watch",
    "test:wasm": "bun test src/__tests__/wasm-wrapper.test.ts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@pbnjam/core": "workspace:*",
    "@pbnjam/codec": "workspace:*",
    "@pbnjam/pvm": "workspace:*",
    "@pbnjam/types": "workspace:*",
    "@pbnjam/pvm-assemblyscript": "workspace:*"
  },
  "devDependencies": {
    "@pbnjam/typescript-config": "workspace:*",
    "bun-types": "1.3.7",
    "vitest": "^2.1.9"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
