{
  "name": "@pbnjam/events",
  "version": "0.1.0",
  "description": "Event system for JAM node services",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --pretty --noEmit",
    "test": "bun test --pass-with-no-tests",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "@pbnjam/core": "workspace:*",
    "@pbnjam/types": "workspace:*"
  },
  "devDependencies": {
    "@pbnjam/typescript-config": "workspace:*",
    "typescript": "^5.0.0",
    "bun-types": "1.3.7"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
